interface:
  display_name: "Oracle APEX Object Lock"
  short_description: "Coordinate shared Oracle DEV object changes"
  default_prompt: "Use $oracle-apex-object-lock to install, verify, and enforce cooperative object locks in shared Oracle DEV."

policy:
  allow_implicit_invocation: true
