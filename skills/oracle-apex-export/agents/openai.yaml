interface:
  display_name: "Oracle APEX Export"
  short_description: "Build APEX baselines, snapshots, and release exports"
  default_prompt: "Use $oracle-apex-export to generate the requested APEX 24.2 baseline, snapshot, or release export while following the project profile."

policy:
  allow_implicit_invocation: true
